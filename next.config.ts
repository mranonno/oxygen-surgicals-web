import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["picsum.photos", "via.placeholder.com"], // add your image hostname here
  },
};

export default nextConfig;
